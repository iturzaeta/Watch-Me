<h1>{{user.username}}</h1>

<div class="d-flex justify-content-start">
    <img class="rounded-circle"src="{{user.avatar}}">
    <div class="ml-5">
    <h3>{{user.name}}</h3>
    <h4 class="mt-4">{{user.email}}</h4>
    <input type="text-area" value="{{user.bio}}">
    </div>
</div>
<div class="mt-4">
<h2>Favorite movies</h2>
</div>



        <div class="d-flex justify-content-start">
    {{#each user.favorite}}
        <div class="d-flex flex-column bd-highlight m-2">
        <h3>{{name}}</h3>
        <img src="{{poster}}">
        </div>
    {{/each}}
        </div>

